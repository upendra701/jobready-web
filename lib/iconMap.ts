import {
  FileText,
  Mic,
  BarChart3,
  PenSquare,
  BookOpen,
  Wallet,
} from "lucide-react";

export const iconMap = {
  file: FileText,
  mic: Mic,
  chart: BarChart3,
  pen: PenSquare,
  book: BookOpen,
  wallet: Wallet,
} as const;